--- origsrc/QScintilla-gpl-2.7.2/Python/configure.py	2013-06-16 08:03:33.000000000 -0500
+++ src/QScintilla-gpl-2.7.2/Python/configure.py	2013-08-16 15:57:51.377009400 -0500
@@ -675,13 +675,17 @@ win32 {
     PY_MODULE = Qsci.pyd
     target.files = Qsci.pyd
     LIBS += -L%s
+} else:cygwin-* {
+    PY_MODULE = Qsci.dll
+    target.files = Qsci.dll
+    LIBS_PRIVATE += -lpython%d.%d%s
 } else {
     PY_MODULE = Qsci.so
     target.files = Qsci.so
 }
 
 target.CONFIG = no_check_exist
-''' % target_config.py_lib_dir)
+''' % (target_config.py_lib_dir, target_config.py_version >> 16, (target_config.py_version >> 8) & 0x0ff, getattr(sys, 'abiflags', '')))
 
     pro.write('''
 target.path = %s
